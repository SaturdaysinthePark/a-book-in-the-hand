# Optional: override the GA4 measurement ID (defaults to the prod stream in src/consts.ts).
# Handy for pointing a preview/staging deploy at a separate GA stream while testing events.
PUBLIC_GA_ID=G-JNR91EN6Y4
